{
  "name": "full-stack-ai-reviewer",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "start": "npm run build:frontend && npm run server:start",
    "server:start": "cd backend && npm install && node server.js",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start:dev": "concurrently \"cd backend && npm run start:dev\" \"cd frontend && npm run dev\"",
    "postinstall": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
